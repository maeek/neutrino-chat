import { ApiAuthorization } from './auth';
import { ApiChannels } from './channels';
import { ApiDevices } from './devices';

export default {
  auth: ApiAuthorization,
  channels: ApiChannels,
  devices: ApiDevices
};
